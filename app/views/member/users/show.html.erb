

<div class = "container">
  <div class = "row">
    <div class = "col mt-2 mb-2 offset-8">
      <%= link_to "プロフィール編集",edit_user_path(@user.id),class: "btn btn-outline-success"%>
    </div>
  </div>
  <div class = "row">
    <div class = "col">
      <%= image_tag @user.get_profile_image(200, 200),class: "img-thumbnail" %>
    </div>
  </div>
  <div class = "row mt-3 mb-5">
    <div class = "col">
      <h3>ユーザーネーム</h3>
    </div>
    <div class = "col">
      <h4><%= @user.name%></h4>
    </div>
  </div>
  <% @posts.each do |post| %>
  <div class = "row">
    <div class = "col">
      <div class="card mb-3">
          <%= link_to post_path(post.id) do %>
            <%if post.images.present? %>
              <% if post.images.attached? %>
                <% post.images.each do |image| %>
                  <%= image_tag image.variant(resize: '200x200') %>
                <% end %>
              <% end %>
            <% else %>
              <%= image_tag "no_images.png" %>
              <% end %>
            <% end %>
          <div class="card-body">
            <h5 class="card-title"><%= link_to post.title,post_path(post.id),style: "color: black;" %></h5>
            <p class="card-text"><%= post.content %></p>
            <p class="card-text">
              <small class="color: black">
                <%= link_to "#{post.comments.count}コメント",post_path(post.id) %>
                👍<%= post.favorites.count %>
              </small>
            </p>
          </div>
      </div>
<% end %>
</div>